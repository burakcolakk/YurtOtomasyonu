server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Bu satır çok önemli: Aranan sayfa yoksa index.html'e yönlendir (React Router için şart)
        try_files $uri $uri/ /index.html;
    }
}